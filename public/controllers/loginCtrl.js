angular.module("movieMe").controller("loginCtrl", function($scope, $rootScope, mainServ) {
  //  $scope.findOrAddUser = function(){
  //    mainServ.findOrAddUser().then( function( user ){
  //      $rootScope.user = user;
  //      console.log( user )
  //    })
  //  }

    // $scope.getUserLocation = function() {
    //     mainServ.getUserLocation().then(function(result) {
    //         $scope.userLocation = result;
    //     })
    // }
    //
    // $scope.getUserLocation();


  //   // Find User's location
  //   $scope.getLoc = navigator.geolocation.getCurrentPosition(function success(position) {
  //   var latitude  = position.coords.latitude;
  //   var longitude = position.coords.longitude;
  //
  //   console.log('<p>Latitude is ' + latitude + '° <br>Longitude is ' + longitude + '°</p>');
  // });


});
